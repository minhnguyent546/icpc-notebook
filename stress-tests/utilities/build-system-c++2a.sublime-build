{
    "cmd": ["g++", "-std=c++2a", "-fmax-errors=5", "-DLOCAL", "-Wall", "-Wextra", "-o", "${file_path}/${file_base_name}.out", "${file}"],
    "file_regex": "^(..[^:]*):([0-9]+):?([0-9]+)?:? (.*)$",
    "working_dir": "${file_path}",
    "selector": "source.cpp, source.c++"
}
